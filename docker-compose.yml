services:
  postgres:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_USER: airesearch
      POSTGRES_PASSWORD: airesearch
      POSTGRES_DB: airesearch
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
